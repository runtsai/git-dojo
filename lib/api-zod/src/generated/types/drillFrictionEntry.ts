/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * Api
 * API specification
 * OpenAPI spec version: 0.1.0
 */

/**
 * Grader friction for one lesson or crisis source
 */
export interface DrillFrictionEntry {
  /** Lesson or crisis id (e.g. "lesson-01", "crisis-03") */
  sourceId: string;
  /** Number of failed grader runs on this source */
  failures: number;
  /** Number of passed grader runs on this source */
  passes: number;
  /** Recency-weighted failure score (0–10 range). Failures decay exponentially with a 14-day half-life so old misses stop dominating the ranking once the learner has improved. */
  effectiveFailures: number;
  /** Passes in the newer half of the last 10 grader runs */
  recentPasses: number;
  /** Failures in the newer half of the last 10 grader runs */
  recentFailures: number;
  /** Passes in the older half of the last 10 grader runs */
  olderPasses: number;
  /** Failures in the older half of the last 10 grader runs */
  olderFailures: number;
  /** Number of failed runs within the rolling window (last 30 grader runs). Use this for display instead of the raw cumulative `failures` counter so the subtitle stays consistent with current struggle level. */
  windowFailures: number;
  /** Number of passed runs within the rolling window (last 30 grader runs). Pair with windowFailures to compute a self-consistent pass ratio (windowPasses / (windowFailures + windowPasses)). */
  windowPasses: number;
  /** True when the learner has fully recovered: recentFailures === 0 and recentPasses > 0. Recovered entries are included in the friction list so the UI can celebrate the improvement before hiding the row. */
  recovered: boolean;
}
